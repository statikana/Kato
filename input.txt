{
    let a = {
        let g = 13;
        let b = {
            let p = 2;
            emit g + p;
        };
        emit b;
    };

    func something(x) {
        return g + x;
    };
    g = 3;
    emit something(3);
};
